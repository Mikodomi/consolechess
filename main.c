#include "chess.h"
#include <stdio.h>

int main() {
    // Board board = starting_board();
    // Board board = empty_board();  

    // // board.pieces[2][2] = make_piece(KING, WHITE, 2, 2);
    // board.pieces[4][3] = make_piece(ROOK, BLACK, 4, 3);
    // board.pieces[7][7] = make_piece(KING, BLACK, 7, 7);
    // printBoard(&board);
    // // printf("%d", is_check(&board, WHITE));
    // printf("%d", is_attacked(&board, 5, 5, WHITE));
    // // Board board = starting_board();
    // // color_type who = WHITE;
    // // while (1) {
    // //     game_turn(&board, who);
    // //     who *= -1;
    // // }
    // // printBoard(&board);
    // // printf("\e[1;1H\e[2J");
    game();
    return 0;
}